// A system font stack instead of a remotely-fetched Google Font: renders
// are self-contained and don't depend on network access at render time.
export const fontFamily =
  '"Helvetica Neue", Helvetica, Arial, "Segoe UI", sans-serif';
